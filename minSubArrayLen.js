/**function should return the minimal length of contiguous subarray of which the sum is greater than
 * or equal to the integer passed to the function.If there isn't one return 0 instead.
 */
function minSubArrayLen(arr, max) {
  if (arr.length === 0) {
    return 0;
  }
}
